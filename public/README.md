# public/

Static files served from the site root.

- `favicon.svg` — browser tab icon (a simple "KR" monogram).
- `og-image.png` — **add this**: a 1200×630 social preview image referenced by
  the `og:image` tag in `index.html`. Until it exists, link previews fall back to
  no image.
